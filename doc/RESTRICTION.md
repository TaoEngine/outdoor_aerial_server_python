# 开始辅助前注意

- 阅读 `.python-version` 了解项目限制版本
- 阅读 `.gitignore` 了解不准读取的文件
- 项目入口在 `main.py`

# 安装模块前注意

- 项目采用 `uv` 管理环境与模块
- 在增删包过程中只准使用 `uv add [模块名]`
- 不要改动 `pyproject.toml` 破坏模块版本
- 不要执行 `uv sync` 破坏模块依赖

# 编写异步前注意

- 项目采用 `trio` 管理异步代码

# 开发链接前注意

- 项目采用 `yarl` 处理链接